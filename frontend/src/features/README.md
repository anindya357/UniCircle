# Feature modules

Each directory owns the components, types, validation, hooks, and tests that are specific to one UniCircle feature. Cross-feature UI belongs in `src/components`; network/mock implementations belong behind contracts in `src/services`.
