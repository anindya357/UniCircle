# Frontend

The Next.js application is initialized in Phase 2. Frontend code uses TypeScript, the App Router, feature modules, shared UI primitives, and a mock service boundary until backend integration begins.

